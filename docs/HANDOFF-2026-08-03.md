# HANDOFF → 2026-08-03

*Written end of 2026-08-02. Verification marks: **[V]** verified by exercising
the machine, **[R]** read from a repo file, **[D]** vendor documentation only,
**[U]** unverified, **[RYAN]** his decision.*

Tomorrow, in order: **push to GitHub**, **verify `crop` on a real reel**, then
**build the Ableton `.als` parse** and the rest of the trigger-map scope.

---

## 0. Cold start — do this before believing anything below

`ls` and `git ls-files` the repo you are about to touch. This project exists
because an unverified assumption about bongpot burned a session, and today it
happened twice more: a Ken Burns capability that turned out to be retired legacy,
and a "Loopback god object" diagnosed from a config file describing devices that
were switched off. **A file read is not ground truth about the machine, and a
negative inference from an absent file is not either.**

Four repos, one content hash (`clip_hash` / SHA-256), zero imports between them:

| Repo | Owns | Remote |
|---|---|---|
| `~/projects/rectum` | capture + clip library | **none — create tomorrow** |
| `~/projects/media-studio` | video finishing, Story IR → OTIO → Resolve | `git@github.com:blessdog/media-studio.git` **[V]** |
| `~/projects/blessdog` | music, SP-404 sample lane (`phase8_sp404`) | `git@github.com:blessdog/blessdog.git` (`feat/sound-control`) **[V]** |
| `~/projects/obs-control-room` | **the** Stream Deck surface (Control Room) | **none — create tomorrow** |

---

## 1. FIRST — push (Ryan, 2026-08-02)

`rectum` and `obs-control-room` have **no git remote at all** **[V]**. Both have
clean trees and one commit each of today's work. A local commit is not a backup.

```
rectum            7fc7244  the clipper — record a monitor, find the video inside it
obs-control-room  71fc207  Control Room grows a second page — rectum
media-studio      3a02bfd  CLIP-LANE — pipeline F moved out to ~/projects/rectum
```

Per the standing keystone rule (asymmetric optionality — a free claim is always
yes): **claim both GitHub names tomorrow**, private, before anything else.
`obs-control-room` also contains no secrets by design — the stream key lives in
OBS's own config, never the repo **[R]** — but re-confirm before pushing public
vs private. **[RYAN]** private or public.

---

## 2. SECOND — the one thing left unverified from today

**`rectum crop` has never been run against a real reel.** It is exact in a clean
case (a 640x360 video inside a 3456x2234 capture found at 1280x720, origin
within 7 px) **[V]**, but on a live desktop the biggest moving region was a
scrolling terminal, not the video **[V]**. It now ranks candidates by density,
aspect and browser-window containment — that ranking is **[U]**.

Thirty seconds, and it is the test that matters:

```
cd ~/projects/rectum
python3 -m rectum record left      # or right — wherever the reel is playing
python3 -m rectum stop
python3 -m rectum crop --last
```

Judge the preview by eye. If a terminal or an editor wins again, make the
browser-bounds filter **hard** (exclude, not weight) rather than tuning scores.

Known trap when testing: everything must actually be **moving and visible**.
Today four captures came back "no moving region found" and every one was
correct — a finished tab, a video blocked from autoplaying because it had sound,
an occluding System Settings window, and Resolve stealing the front. **Look at a
frame of the capture before blaming the detector.**

Also: the Loopback device `Clip Capture` records **all** of Brave, so pause
background tabs or music bleeds into every sample **[V]**.

---

## 3. THIRD — the Ableton `.als` parse (pipeline G2)

The trigger map answers *where in the finished track does each sample fire*, so
the meme's own frames can land on the Resolve timeline at that beat. Three
possible producers (CLIP-LANE §4); **G2 is the one buildable now** — no SP-404 on
the desk, no hardware, no routing.

### Verified today against Ryan's own project **[V]**

`~/Music/Ableton/Live Recordings/thunderdome Project/thunderdome.als`
(188 `.als` on disk):

- **gzipped XML**, Ableton Live 12.4 — `gzip` + any XML parser, no Ableton
  automation, no MCP.
- Contains `<AudioClip>`, `<MidiClip>`, `<CurrentStart>`, `<CurrentEnd>`,
  `<SampleRef>`, `<MidiNoteEvent>`.
- **Times are in BEATS, not seconds.** `<CurrentEnd Value="3852.032">` at
  tempo 120 = 1926 s ≈ 32 min, which matches a long mastered rip. So
  `seconds = beats * 60 / bpm`.
- **Master tempo is readable** — `<Tempo>` → `<Manual Value="120">`.
- **`<SampleRef><FileRef>` carries an absolute `<Path Value="/Users/…">`** as
  well as a `<RelativePath>`. **The absolute path is the join**: hash that file,
  look it up in rectum's ledger by `clip_hash`.

### Unknowns — resolve each BY EXERCISING, not by reading

1. **Which `<Tempo>` is the master?** There are **11** `<Tempo>` elements in one
   project **[V]**. Picking the wrong one silently scales the whole timeline.
2. **Is there tempo automation?** If tempo varies, `beats → seconds` is not one
   multiply and needs the tempo envelope integrated. Check before assuming.
3. **Arrangement vs Session clips.** A finished track is the **Arrangement**.
   Session-view clip slots must be excluded or they produce phantom placements
   at bogus times.
4. **Warp / clip start offset.** `<CurrentStart>` is position on the timeline;
   the clip's own start *inside the sample* is what feeds `source_in_secs` — the
   field that makes the picture chop in sync with the audio. They are different
   numbers; do not conflate them.
5. **Chopped samples** should appear as many `<AudioClip>` entries referencing
   one file. That *is* the trigger map. Confirm on a project that actually has
   chops — `thunderdome` has only 1 AudioClip and 1 MidiClip **[V]**, so it
   proves the format but **not** the chop case. Ask Ryan for a chopped set, or
   make one.

### Output contract — one artifact, three possible producers

```json
{ "version": 1, "bpm": 120, "source": "thunderdome.als",
  "entries": [
    { "clip_hash": "…", "source_path": "/Users/…/x.wav",
      "track_start_secs": 42.15, "duration_secs": 0.5,
      "source_in_secs": 2.1, "kind": "stab" }
  ] }
```

**Beds are not stabs.** Ryan's Tyson concept — fight ambience under the beat,
faded up at chosen moments — means an entry needs a **duration and a level
envelope**, not just an onset, and its matching video is a long clip under the
section rather than a stab on a beat. Both must round-trip. Test material:
`https://www.youtube.com/watch?v=nm0OjxUEaSk` (bed) and
`https://www.youtube.com/watch?v=Fp9xuYeJako` (track) **[RYAN]**.

**READ ONLY.** Never write into an Ableton project. Same one-way rule as
Resolve: scripts never touch a human-edited timeline.

### Where it lives — **[RYAN]** decision

The parser reads a *music* artifact and emits a *video* edit instruction.
`blessdog` owns music, `media-studio` owns video. Recommendation: **the parser
lives in `blessdog`** (it understands tempo and sample files), and emits
`trigger-map.json`, which `media-studio` consumes. The boundary stays the file.

---

## 4. The rest of the scope — trigger map → Resolve (pipeline H)

Once `trigger-map.json` exists, the consumer:

```
trigger-map.json + finished track + stems
  → resolve clip_hash → rectum ledger → source clip + crop + source_in_secs
  → place each video clip at track_start_secs, trimmed to the SAME source range
  → tools/compile-ir.py → OTIO → Resolve → Ryan's GUI pass    EXISTS [R]
```

> ### ✅ ALL THREE PIPELINE-D GAPS ARE BUILT (2026-08-02 evening) — do NOT rebuild
>
> This section listed them as open. They shipped later the same day, all
> exercised against live Resolve 21.0.2.4:
>
> 1. **`tools/beat-grid.py --bpm 174 [--first-beat S]`** (`8d2f66c`) — computes
>    the grid arithmetically, skipping librosa. `beats.json` records
>    `bpmSource` ("declared" vs "librosa"). Measured: on a *perfect* 120 BPM
>    click track librosa returns **117.45** and finds 22 of 24 beats — ~4s drift
>    over 3 minutes. Also fixed a silent failure where librosa returned
>    `bpm 0.0` with an EMPTY beat list on untrackable material.
> 2. **`tools/ingest-song.py`** (`c9a1c07`) — the song front door. Song whole
>    and uncut on A1, stems on A2+ each with its own length, beat grid inline.
>    Verified in Resolve: V0/A3, 6 markers at frames 0/60/120/180/240/300.
> 3. **`tools/edit-ir.py add-stems`** (`1293f64`) — **and the "audio-lane schema
>    bump" was never needed.** `studio/emit.py:32` already routed audio edits by
>    lane, `add_music` already took `track`, the schema's `track` was already
>    generic. `COMPILER_EPOCH` stays at **2**; `docs/PLAN.md:77` corrected in
>    place. Only CLI surface was missing.
>
> Voice on A1 is still sacred; `add_stems` refuses `first_track < 2`.

**The SP-404 library builder is also proven on real audio** (`blessdog@edcf87b`,
55 tests): 10 samples / 476 MB in `~/projects/blessdog/music/sp404/`, ledger at
`music/sp404-library.json`. `--analyze` detects BPM + key (Dirty Diana → 129.2
BPM / Gm, matching documented values) and **suppresses key below 0.45
confidence** because a drums stem returned a confident-looking "D#m" at 0.287.
Percussion reports tempo only. Remaining gaps are listed in STATUS.md
§"Pipeline A — what is still NOT done".

Do **not** automate punch-ins or Ken Burns moves. Auto-crop is mechanical and
safe; camera moves are creative and belong in Resolve with Ryan's hands on them.
`copilot, not autopilot`.

---

## 5. State of rectum (what tomorrow builds on)

**Working, exercised end to end [V]:** monitor capture with audio (left/right
derived live from `NSScreen` origin.x, never hardcoded), VFR → CFR normalise,
SHA-256, 48 kHz/16-bit WAV extraction (exactly what the SP-404 imports), JSON
ledger, substring search, Stream Deck keys on Control Room page 2.

**Partial:** `crop` — see §2.

**Not started:** transcription. This is the piece that makes the library actually
searchable — meme clips are mostly speech, so "find the one where he says…" is
the only search anyone runs. `tools/ingest-recording.py` already has a Deepgram
leg **[R]** and Audio Hijack ships a local non-beta Transcribe block **[V]**.
The ledger already has an empty `transcript` field waiting.

### Traps this machine has already sprung — do not rediscover them

- A GUI-launched process inherits a **minimal PATH**. `ffmpeg` (Homebrew) is not
  on it, and a bare `python3` is Apple's, which has **no numpy** **[V]**. Both
  are handled now; keep it that way when adding entry points.
- Spawn long-running recorders with `DEVNULL` + `start_new_session`, or a caller
  that captures output blocks forever on a pipe the recorder holds **[V]**.
- `ffmpeg … volumedetect` must **not** be run with `-v error` — it suppresses the
  measurement, which then reads as "no audio" **[V]**.
- **Measure loudness, never infer it.** A capture from a device with nothing
  routed produced a flawless h264+aac file at **−91 dB** **[V]**.
- `screencapture -R` is **global desktop points**, top-left origin, negative x
  valid; output **pixels** are points × that display's backing scale **[V]**.
- Config-by-database apps (Stream Deck, Loopback, Companion) **rewrite their
  files from memory on quit**. Quit first, never force-kill **[R]**.
- `build-profile.mjs` needs one-time **Automation permission** to quit the Stream
  Deck app, or Ryan quits it by hand **[V]**.

### Two things I got wrong today, recorded so they are not repeated

1. Claimed **automatic gain control** from two captures at identical levels — it
   was a confound; a constant background signal swamped both tests. **Run the
   silent control first.** Measuring carefully is not measuring the right thing.
2. Concluded Loopback was **not installed** from an absent HAL driver. `ARK.driver`
   *is* Loopback's driver (shared across the Rogue Amoeba suite). The devices
   were simply switched **off**. Corrections are in `MUSIC-LANE.md` §4.
